.idea/
.DS_Store
CLAUDE.md
HANDOFF.md
_tmp_preview*.html

# Working notes from the plan/spec skills. This repo is public and renders on the
# GitHub profile; planning artefacts belong in the private knowledge repo, the same
# rule CLAUDE.md follows.
docs/superpowers/

# Generated by scripts/build.mjs and rebuilt by Cloudflare Pages on every deploy.
# Committing them duplicated every asset; the build command is set, so CI makes them.
/index.html
/output/
